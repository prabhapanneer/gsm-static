{
  "name": "gsm-contact-gateway",
  "private": true,
  "type": "module",
  "description": "GSM website — POST /contact, sends enquiry email via SMTP (nodemailer). Run: npm run dev",
  "scripts": {
    "dev": "node index.mjs",
    "start": "node index.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemailer": "^6.9.15"
  }
}
